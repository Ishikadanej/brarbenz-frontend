import React from "react";
import AccountLayoutClient from "../../../imports/account/layout/AccountLayoutClient";

export default function AccountLayout({ children }) {
  return <AccountLayoutClient>{children}</AccountLayoutClient>;
}
